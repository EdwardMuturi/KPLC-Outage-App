CREATE TABLE part (
name TEXT NOT NULL PRIMARY KEY,
regionId TEXT NOT NULL
);

tAll:
SELECT *
FROM part;

findById:
SELECT *
FROM part
WHERE name = ?;

findByRegionId:
SELECT *
FROM part
WHERE regionId = ?;

insert:
INSERT INTO part(name, regionId)
VALUES ?;