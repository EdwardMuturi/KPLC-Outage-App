CREATE TABLE region (
name TEXT NOT NULL);
selectAll:
SELECT *
FROM region;

findByName:
SELECT *
FROM region
WHERE name = ?;

insert:
INSERT OR REPLACE INTO region(name)
VALUES (?);

deleteAll:
DELETE FROM region;