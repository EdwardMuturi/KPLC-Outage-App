CREATE TABLE area (
name TEXT NOT NULL,
partId TEXT NOT NULL,
regionId TEXT NOT NULL
);

tAll:
SELECT *
FROM area;

findByRegionId:
SELECT *
FROM area
WHERE regionId = ?;

insert:
INSERT OR REPLACE INTO area(name, partId, regionId)
VALUES ?;

deleteAll:
DELETE FROM area;